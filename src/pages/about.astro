---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
	title="关于这个网站 · 269332"
	description="了解 269332 个人网站的项目、守望先锋工具、技术架构和源码信息。"
>
	<main class="about-page">
		<header class="about-hero">
			<p class="eyebrow">关于</p>
			<h1>关于这个网站</h1>
			<p class="lead">269332 是一个用于整理个人项目、守望先锋工具和开发经验的独立网站。</p>
			<p>
				这里的内容主要来自实际开发和问题解决过程，重点是把零散经验整理成能够长期保存和继续维护的内容。
			</p>
			<nav aria-label="本页内容">
				<a href="#content">网站内容</a>
				<a href="#principles">项目原则</a>
				<a href="#technology">网站技术</a>
				<a href="#source">源码与反馈</a>
			</nav>
		</header>

		<section id="content" aria-labelledby="content-title">
			<p class="eyebrow">内容方向</p>
			<h2 id="content-title">网站内容</h2>
			<div class="content-grid">
				<article>
					<h3>Windows 软件与系统工具</h3>
					<p>整理电源设置、网络控制和 Windows 系统能力相关的桌面项目。</p>
				</article>
				<article>
					<h3>守望先锋工具箱</h3>
					<p>收录赛事预测、英雄猜名和其他围绕《守望先锋》制作的浏览器交互工具。</p>
				</article>
				<article>
					<h3>网站开发与部署</h3>
					<p>围绕 Astro、GitHub、Cloudflare 和自动化构建维护这个站点。</p>
				</article>
				<article>
					<h3>游戏与电竞开发</h3>
					<p>围绕赛事结构、交互展示和游戏内容制作可继续维护的项目。</p>
				</article>
			</div>
			<p class="content-note">文章功能正在整理，完善后会重新开放。</p>
		</section>

		<section id="principles" aria-labelledby="principles-title">
			<p class="eyebrow">维护方式</p>
			<h2 id="principles-title">内容与项目原则</h2>
			<ul class="principle-list">
				<li><span>01</span>只展示真实存在的项目和守望先锋工具。</li>
				<li><span>02</span>尽量提供清楚的功能说明和源码入口。</li>
				<li><span>03</span>优先解决实际使用中的问题。</li>
				<li><span>04</span>保持网站结构简单、轻量和可维护。</li>
				<li><span>05</span>不为了填充页面编造项目、数据和成果。</li>
				<li><span>06</span>内容随着项目开发逐步更新。</li>
			</ul>
		</section>

		<section id="technology" aria-labelledby="technology-title">
			<p class="eyebrow">技术架构</p>
			<h2 id="technology-title">网站技术</h2>
			<div class="architecture">
				<dl>
					<div><dt>生成方式</dt><dd>Astro 静态生成</dd></div>
					<div><dt>开发语言</dt><dd>TypeScript、HTML、CSS</dd></div>
					<div><dt>内容能力</dt><dd>Markdown 与 MDX</dd></div>
					<div><dt>源码管理</dt><dd>GitHub</dd></div>
					<div><dt>构建部署</dt><dd>Cloudflare 自动部署</dd></div>
					<div><dt>正式域名</dt><dd><a href="https://269332.xyz">269332.xyz</a></dd></div>
				</dl>
				<p>
					网站保持静态输出，不依赖长期运行的应用服务器。源码推送到 GitHub 后，由 Cloudflare 完成构建和发布。
				</p>
			</div>
		</section>

		<section id="source" aria-labelledby="source-title">
			<p class="eyebrow">开放信息</p>
			<h2 id="source-title">源码与反馈</h2>
			<p>
				可以通过 GitHub 查看网站源码、相关项目和开发进度。发现页面错误或失效链接时，可先在对应仓库查看已有说明和反馈渠道。
			</p>
			<div class="source-links">
				<a
					class="source-card"
					href="https://github.com/yundan125/269332-site"
					target="_blank"
					rel="noopener noreferrer"
				>
					<span>网站源码</span>
					<strong>yundan125/269332-site</strong>
					<small>在 GitHub 查看 <span aria-hidden="true">↗</span></small>
					<span class="sr-only">（在新窗口打开）</span>
				</a>
				<a
					class="source-card"
					href="https://github.com/yundan125"
					target="_blank"
					rel="noopener noreferrer"
				>
					<span>GitHub 主页</span>
					<strong>github.com/yundan125</strong>
					<small>查看全部公开仓库 <span aria-hidden="true">↗</span></small>
					<span class="sr-only">（在新窗口打开）</span>
				</a>
			</div>
		</section>

		<aside class="site-note" aria-labelledby="site-note-title">
			<h2 id="site-note-title">站点说明</h2>
			<ul>
				<li>本站为个人网站，页面内容可能随项目更新而调整。</li>
				<li>外部链接会在新窗口打开。</li>
				<li>项目授权以对应仓库中的许可证为准。</li>
			</ul>
		</aside>
	</main>
</BaseLayout>

<style>
	.about-page {
		width: min(960px, calc(100% - 2rem));
		max-width: none;
	}
	.about-hero {
		max-width: 780px;
		padding-bottom: clamp(3rem, 7vw, 5rem);
	}
	.about-hero h1 {
		margin-bottom: 1.25rem;
	}
	.about-hero .lead {
		color: var(--color-text);
		font-size: clamp(1.18rem, 2.5vw, 1.4rem);
		font-weight: 700;
		line-height: 1.55;
	}
	.about-hero > p:not(.eyebrow) {
		max-width: 44rem;
	}
	.about-hero nav {
		display: flex;
		flex-wrap: wrap;
		gap: 0.55rem;
		margin-top: 2rem;
	}
	.about-hero nav a {
		padding: 0.35rem 0.65rem;
		background: var(--color-surface);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-small);
		color: var(--color-text-muted);
		font-size: 0.78rem;
		font-weight: 700;
		text-decoration: none;
	}
	.about-page > section {
		padding: clamp(3rem, 7vw, 5rem) 0;
		border-top: 1px solid var(--color-border);
		scroll-margin-top: 6rem;
	}
	.content-grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1rem;
		margin-top: 2rem;
	}
	.content-grid article {
		padding: 1.35rem;
		background: var(--color-surface);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-medium);
	}
	.content-grid h3 {
		font-size: 1.08rem;
	}
	.content-grid p,
	.content-note {
		margin: 0;
		color: var(--color-text-muted);
		font-size: 0.88rem;
	}
	.content-note {
		margin-top: 1rem;
		padding: 0.85rem 1rem;
		background: var(--color-accent-soft);
		border-radius: var(--radius-small);
		color: var(--color-accent-hover);
	}
	.principle-list {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 0;
		margin: 2rem 0 0;
		padding: 0;
		border-top: 1px solid var(--color-border);
		list-style: none;
	}
	.principle-list li {
		display: flex;
		gap: 1rem;
		padding: 1rem 0;
		border-bottom: 1px solid var(--color-border);
	}
	.principle-list li:nth-child(odd) {
		padding-right: 1.5rem;
	}
	.principle-list li:nth-child(even) {
		padding-left: 1.5rem;
		border-left: 1px solid var(--color-border);
	}
	.principle-list span {
		color: var(--color-accent);
		font-size: 0.7rem;
		font-weight: 700;
	}
	.architecture {
		display: grid;
		grid-template-columns: minmax(0, 1.25fr) minmax(16rem, 0.75fr);
		gap: 2rem;
		margin-top: 2rem;
	}
	dl {
		margin: 0;
		background: var(--color-surface);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-medium);
	}
	dl div {
		display: grid;
		grid-template-columns: 7rem 1fr;
		gap: 1rem;
		padding: 0.75rem 1rem;
	}
	dl div + div {
		border-top: 1px solid var(--color-border);
	}
	dt {
		color: var(--color-text-muted);
		font-size: 0.78rem;
		font-weight: 700;
	}
	dd {
		margin: 0;
		font-size: 0.88rem;
		font-weight: 700;
	}
	.architecture > p {
		margin: 0;
		color: var(--color-text-muted);
	}
	#source > p:not(.eyebrow) {
		max-width: 44rem;
		color: var(--color-text-muted);
	}
	.source-links {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1rem;
		margin-top: 2rem;
	}
	.source-card {
		display: flex;
		flex-direction: column;
		padding: 1.35rem;
		background: var(--color-surface);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-medium);
		color: var(--color-text);
		text-decoration: none;
	}
	.source-card:hover {
		border-color: var(--color-accent);
		box-shadow: var(--shadow-small);
		transform: translateY(-2px);
	}
	.source-card > span:first-child,
	.source-card small {
		color: var(--color-text-muted);
		font-size: 0.72rem;
	}
	.source-card strong {
		margin: 0.25rem 0 1.5rem;
		overflow-wrap: anywhere;
	}
	.source-card small {
		margin-top: auto;
		color: var(--color-accent);
		font-weight: 700;
	}
	.site-note {
		padding: 1.5rem;
		background: #152130;
		border-radius: var(--radius-medium);
		color: #bdc8d5;
	}
	.site-note h2 {
		color: white;
		font-size: 1.1rem;
	}
	.site-note ul {
		margin: 0;
		padding-left: 1.2rem;
		font-size: 0.85rem;
	}
	@media (max-width: 720px) {
		.about-page {
			width: min(960px, calc(100% - 1.5rem));
		}
		.content-grid,
		.principle-list,
		.architecture,
		.source-links {
			grid-template-columns: 1fr;
		}
		.principle-list li:nth-child(odd),
		.principle-list li:nth-child(even) {
			padding-right: 0;
			padding-left: 0;
			border-left: 0;
		}
	}
	@media (prefers-reduced-motion: reduce) {
		.source-card:hover {
			transform: none;
		}
	}
</style>
